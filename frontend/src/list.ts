/// <reference path="../typings/index.d.ts" />
'use strict';

angular.
    module('timelapseApp').
    component('timelapseList', {
        templateUrl: '../templates/list.html',
        controller: [() => {
            let ctrl = this;
        }]
    });
